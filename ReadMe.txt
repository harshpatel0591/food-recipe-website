Follow below steps to run the website

- Install MS SQL Server.
- Load the Foodie_Database(.bak File) in the SQL Server. 
- Connect the Database with the Web application(Yummly) by saving/updating the Database credentials in the AppSettings of the Web.config file present in the Web application(Yummly).
- Run the Web site.